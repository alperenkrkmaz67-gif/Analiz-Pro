// db.js - User Management Only (Match data removed)
// Match data is now handled by excelLoader.js (client-side)

const DB = {
    // User management functions can be added here if needed
    // Currently empty as user auth is handled by auth.js
};

window.DB = DB;